# Rust build output. The vendored crate has its own Cargo target directory.
/target/
/vendor/*/target/
**/target/
**/*.rs.bk

# JavaScript dependency trees. Individual apps may add narrower frontend
# outputs in their own ignore files.
node_modules/

# Python tooling caches produced by packaging helpers.
__pycache__/
*.py[cod]

# Local annotation sidecars and interrupted atomic-save temporaries.
*.gpui-pdf-reader.json
*.gpui-pdf-reader.json.tmp-*

# macOS filesystem metadata.
.DS_Store
.AppleDouble
.LSOverride

# Local editor state.
.idea/
.vscode/
*.swp
*.swo
*~

# Local environment files may contain machine-specific paths or secrets.
.env
.env.*
!.env.example

# Local build-in-public content planning.
/content/
