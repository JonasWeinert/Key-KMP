{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities required by the isolated PDF experiment",
  "windows": ["main", "pdf-renderer-*"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
